<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="OmniConsole.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OmniConsole"
    xmlns:svc="using:OmniConsole.Services">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
                <!-- Other merged dictionaries here -->
            </ResourceDictionary.MergedDictionaries>

            <!-- 全域 ComboBox / AutoSuggestBox：展開下拉/建議清單時把游標設回箭頭，根治忙碌轉圈，手把+滑鼠皆涵蓋。
                 透過 AutoFix attached property 套用；BasedOn 內建 style 保留外觀。 -->
            <Style TargetType="ComboBox" BasedOn="{StaticResource DefaultComboBoxStyle}">
                <Setter Property="svc:BusyCursorFix.AutoFix" Value="True"/>
            </Style>
            <Style TargetType="AutoSuggestBox" BasedOn="{StaticResource DefaultAutoSuggestBoxStyle}">
                <Setter Property="svc:BusyCursorFix.AutoFix" Value="True"/>
            </Style>

            <!-- NavigationView 內容區與頂部面板設為透明，讓底層背景材質透出。
                 面板主背景 NavigationViewDefaultPaneBackground 不在此設，由 BackgroundMaterialService
                 於 App 啟動時依背景材質覆寫。 -->
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Default">
                    <SolidColorBrush x:Key="NavigationViewTopPaneBackground" Color="Transparent"/>
                    <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent"/>
                </ResourceDictionary>
                <ResourceDictionary x:Key="Dark">
                    <SolidColorBrush x:Key="NavigationViewTopPaneBackground" Color="Transparent"/>
                    <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent"/>
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>
